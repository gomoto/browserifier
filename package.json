{
  "name": "browserifier",
  "version": "0.2.1",
  "description": "Browserify boilerplate",
  "main": "browserifier.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gomoto/browserifier.git"
  },
  "scripts": {
    "postinstall": "typings install",
    "version": "tsc && git add -A",
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "browserify": "^13.1.0",
    "object-assign": "^4.1.0",
    "tsify": "^1.0.1",
    "uglifyify": "^3.0.2",
    "watchify": "^3.7.0"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  }
}
